{% extends 'base.html.twig' %}

{% block stylesheet %}
<!-- CSS rules for styling the element inside the editor such as p, h1, h2, etc. -->
<link href="../css/froala_style.min.css" rel="stylesheet" type="text/css" />
{% endblock %}
{% block body %}
<h1>Formulaire Ajout d'une publication</h1>

<section id="main-content">
        <section class="wrapper">

            <h3><i class="fa fa-angle-right"></i> Edition d'un projet</h3>

            <div class="row">
                <div class="col-lg-12">
                    <div class="row">
                        <div class="form-panel">
                            <form action="" class="form-horizontal style-form" method="POST">
                                <div class="form-group">
                                    <label class="col-sm-2 col-sm-2 control-label">Titre : </label>
                                    <div class="col-sm-10">
                                        {{ form_errors(form.titre) }}
                                        {{ form_widget(form.titre, {'attr' : {'class' : 'form-control', }}) }}
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="col-sm-2 col-sm-2 control-label">Votre Article : </label>
                                    <div class="col-sm-10 fr-view">
                                        {{ form_errors(form.content) }}
                                        {{ form_widget(form.content, {'attr' : {'class' : 'form-control'}}) }}
                                    </div>
                                </div>
                                {{form_rest(form)}}
                                <input type="submit" name="submit" value="Enregistrer" class="btn btn-sm btn-success"/>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
            <!--/row -->
        </section>
    </section>
{% endblock %}